<span>
    @if ($count > 0)
        <span
            class="ml-1 inline-flex items-center justify-center rounded-full bg-neutral-900 px-1.5 py-0.5 text-xs font-semibold text-white dark:bg-white dark:text-neutral-900"
        >{{ $count }}</span>
    @endif
</span>
